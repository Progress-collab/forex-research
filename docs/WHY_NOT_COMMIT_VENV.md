# Важно: Почему .venv и .env НЕ должны быть в Git

## ❌ НЕ коммитить в Git:

### 1. `.venv/` (виртуальное окружение)
**Почему НЕ нужно:**
- Размер: сотни MB (замедляет git)
- Зависит от ОС (Linux/Mac/Windows разные)
- Можно пересоздать: `pip install -e ".[backtesting]"`
- Уже правильно игнорируется в `.gitignore`

### 2. `.env` (секретные данные)
**Почему НЕ нужно:**
- Содержит API ключи и пароли
- Безопасность! Нельзя публиковать секреты
- Уже правильно игнорируется в `.gitignore`

### 3. `data/` (данные)
**Почему НЕ нужно:**
- Очень большие файлы (GB)
- Git не для бинарных файлов
- Используйте Git LFS или внешнее хранилище

## ✅ Что ДОЛЖНО быть в Git:

1. Весь исходный код (`src/`, `scripts/`)
2. Конфигурация (`pyproject.toml`, `config/`)
3. Документация (`docs/`)
4. CI/CD (`.github/workflows/`)
5. Примеры конфигов (`.env.example`)

## 📋 Что делать для работы в облаке:

### 1. Создать `.env.example` (пример без секретов):
```bash
# Пример конфигурации (без реальных значений)
CTRADER_CLIENT_ID=your_client_id_here
CTRADER_CLIENT_SECRET=your_secret_here
CTRADER_ACCESS_TOKEN=your_token_here
```

### 2. Данные хранить отдельно:
- Внешнее хранилище (S3, Google Cloud Storage)
- Или скачивать при деплое
- Или использовать Git LFS для небольших файлов

### 3. Виртуальное окружение создавать при деплое:
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[backtesting]"
```

## Текущий статус:

✅ `.gitignore` правильно настроен
✅ `.venv/` игнорируется
✅ `.env` игнорируется  
✅ `data/` игнорируется
✅ Все важные файлы проекта в git

**Вывод:** Всё настроено правильно! Не нужно коммитить .venv и .env.
