# Инструкция: Пошаговая настройка Git LFS и добавление данных

## ✅ Шаг 1: Git LFS установлен и настроен

Git LFS уже установлен (версия 3.7.1) и инициализирован.
`.gitattributes` настроен для отслеживания `.parquet` файлов.

## 📋 Шаг 2: Скопировать данные с локального Mac

Выполните на вашем Mac в терминале:

```bash
# 1. Перейти в директорию проекта (если нужно)
cd /workspace

# 2. Создать директорию для данных (если еще не создана)
mkdir -p data/v1/curated/ctrader

# 3. Скопировать данные из локальной папки
cp "/Users/evgenyglazeykin/Yandex.Disk-eglazejkin.localized/Cursor_happy/data/v1/curated/ctrader"/*.parquet data/v1/curated/ctrader/

# 4. Проверить что файлы скопированы
ls -lh data/v1/curated/ctrader/
```

## 📋 Шаг 3: Добавить данные в Git (через LFS)

После копирования данных выполните:

```bash
# 1. Добавить данные в git (Git LFS автоматически обработает их)
git add data/v1/curated/ctrader/*.parquet

# 2. Проверить что файлы будут через LFS
git lfs ls-files

# 3. Закоммитить
git commit -m "Add forex data files via Git LFS"

# 4. Запушить (данные будут загружены через LFS)
git push
```

## ✅ Шаг 4: Проверка

После push проверьте:

```bash
# Проверить что данные через LFS
git lfs ls-files

# Запустить тест стратегии
python3 scripts/check_and_test_momentum.py
```

## 📊 Текущий статус

- ✅ Git LFS установлен и инициализирован
- ✅ .gitattributes настроен
- ✅ .gitignore обновлен для работы с LFS
- ⏳ Ожидает копирования данных с локального Mac
- ⏳ Ожидает добавления данных в git

## 💡 Что дальше?

1. Скопируйте данные (Шаг 2)
2. Добавьте в git (Шаг 3)
3. Запушите на GitHub
4. Данные будут доступны в облаке!
