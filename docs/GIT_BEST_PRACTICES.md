# Важно: Что НЕ нужно коммитить в Git

## ❌ НЕ коммитить (уже настроено в .gitignore):

1. **`.venv/`** - виртуальное окружение
   - Слишком большой (сотни MB)
   - Зависит от операционной системы
   - Можно пересоздать через `pip install -e ".[backtesting]"`

2. **`.env`** - секретные данные
   - Содержит API ключи и пароли
   - Безопасность!
   - Используйте `.env.example` для примера

3. **`data/`** - данные
   - Очень большие файлы (GB)
   - Git не предназначен для больших бинарных файлов
   - Используйте Git LFS или внешнее хранилище

4. **`__pycache__/`**, `*.pyc` - скомпилированный Python код
   - Автоматически генерируется
   - Не нужен в репозитории

## ✅ Что ДОЛЖНО быть в Git:

1. **Весь исходный код** (`src/`, `scripts/`)
2. **Конфигурационные файлы** (`pyproject.toml`, `config/`)
3. **Документация** (`docs/`)
4. **CI/CD конфигурация** (`.github/workflows/`)
5. **Примеры конфигов** (`.env.example`, `secrets/*.example.json`)

## 📋 Рекомендации:

### Для работы в облаке:

1. **Создайте `.env.example`** с примерами переменных (без реальных значений):
   ```bash
   CTRADER_CLIENT_ID=your_client_id_here
   CTRADER_CLIENT_SECRET=your_secret_here
   ```

2. **Данные храните отдельно**:
   - Используйте внешнее хранилище (S3, Google Cloud Storage)
   - Или Git LFS для небольших файлов
   - Или скачивайте данные при деплое

3. **Виртуальное окружение создавайте при деплое**:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -e ".[backtesting]"
   ```

### Текущий статус:

✅ `.gitignore` правильно настроен
✅ `.venv/` и `.env` игнорируются
✅ `data/` игнорируется
✅ Все важные файлы проекта должны быть в git
