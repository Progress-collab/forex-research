# ⚠️ ВАЖНО: Шаги 2-4 нужно выполнить на вашем Mac!

Я не могу напрямую получить доступ к вашей локальной папке с данными, поэтому нужно выполнить это вручную.

## 🚀 Быстрый способ: Использовать готовый скрипт

На вашем Mac выполните:

```bash
cd /workspace
bash scripts/do_steps_2_3_4.sh
```

Этот скрипт автоматически выполнит все шаги 2-4.

## 📋 Или выполните вручную:

### Шаг 2: Копирование данных

```bash
# Перейти в директорию проекта
cd /workspace

# Создать папку для данных
mkdir -p data/v1/curated/ctrader

# Скопировать данные
cp "/Users/evgenyglazeykin/Yandex.Disk-eglazejkin.localized/Cursor_happy/data/v1/curated/ctrader"/*.parquet data/v1/curated/ctrader/

# Проверить
ls -lh data/v1/curated/ctrader/
```

### Шаг 3: Добавить в Git LFS

```bash
# Добавить данные в git (Git LFS автоматически обработает их)
git add data/v1/curated/ctrader/*.parquet

# Проверить что файлы будут через LFS
git lfs ls-files
```

### Шаг 4: Коммит и push

```bash
# Закоммитить
git commit -m "Add forex data files via Git LFS"

# Запушить (данные будут загружены через LFS)
git push
```

## ✅ После выполнения

После push данные будут в облаке! Проверьте:

```bash
# Проверить что данные через LFS
git lfs ls-files

# Запустить тест стратегии
python3 scripts/check_and_test_momentum.py
```

## 💡 Альтернатива: Загрузка через API

Если не можете скопировать данные, можно загрузить через API:

```bash
# Настроить переменные окружения в .env
# Затем:
python3 scripts/download_data_on_deploy.py
```

Но для этого нужны credentials для cTrader API.
