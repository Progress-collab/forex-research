# Быстрый старт - выполнение плана

## 🎯 Варианты запуска

### Вариант 1: Локально (рекомендуется для начала)

```bash
# Установка зависимостей (без venv)
pip3 install --upgrade pip
pip3 install -e ".[backtesting]"

# Проверка что всё работает
python3 -c "import pandas; import numpy; import src.data_pipeline; print('✅ OK')"
```

### Вариант 2: С виртуальным окружением

```bash
# Если есть python3-venv
./scripts/setup_local.sh
```

## 📋 План выполнения (Итерация 1)

Согласно `docs/forex_system/iteration_plan.md`:

### 1. Стабилизировать Prefect-потоки
```bash
# Проверить существующие потоки
ls research/flows/
python3 -m research.flows.daily_backtests
```

### 2. Автоматизировать выгрузку макроданных
```bash
# Обновить экономический календарь
python3 scripts/update_economic_calendar.py
```

### 3. Параметрическая оптимизация стратегий
```bash
# Оптимизация стратегии
python3 scripts/optimize_strategy.py

# Walk-forward валидация
python3 scripts/walk_forward_validation.py
```

## 🚀 Быстрый тест

```bash
# 1. Проверка данных
python3 scripts/run_ingest.py

# 2. Запуск бэктеста
python3 scripts/run_full_backtests.py

# 3. Анализ результатов
python3 scripts/analyze_backtest_results.py
```

## 📊 Текущее состояние

Согласно `docs/plan_execution_summary.md`:
- ✅ Бэктесты запущены
- ✅ Анализ результатов выполнен
- ⚠️ Стратегии требуют доработки (убыточные)
- 🔄 Оптимизация в процессе

## 🔧 Cloud Build

Если нужен cloud build:
- Упрощенный workflow создан: `.github/workflows/ci.yml`
- Запускается автоматически при push
- Можно запускать локально без облака
